server:
  port: 9000

spring:
  application:
    name: EMAIL-SERVICE

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
    enabled: true
