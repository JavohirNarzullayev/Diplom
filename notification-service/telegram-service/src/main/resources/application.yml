server:
  port: 9091

spring:
  application:
    name: TELEGRAM-SERVICE

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
    enabled: true